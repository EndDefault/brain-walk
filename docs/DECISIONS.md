# 결정 기록

## 2026-09-22 · 첫 PR

- **공개 저장소 사용:** 사용자 후속 지시가 최초 명세의 비공개 원칙보다 우선합니다. `EndDefault/brain-walk`를 Public으로 유지합니다.
- **원격 초기 이력 유지:** 기존 `main`의 `aa4e485`는 README만 포함한 초기 커밋입니다. 이를 부모로 사용하며 기존 `.idea/` 파일은 삭제하거나 변경하지 않습니다.
- **첫 PR 범위:** 빌드·기본 실행·홈 빈 상태·이용 안내·화면 이동 확인까지입니다. 훈련 유형 카드는 안내이며 누르는 버튼이 아닙니다. 게임 시작, 그래프, 가짜 성적은 제공하지 않습니다.
- **이름:** 기존 앱이나 Android 패키지가 없어 명세의 임시 프로젝트 이름 `MemorySteps`, 패키지 `com.example.memorysteps`를 사용합니다. 저장소 이름 `brain-walk`는 유지합니다. 화면에는 한국어 이름 ‘기억 산책’과 MemorySteps를 함께 표시합니다.
- **커밋 단위:** 기준 명세, 빌드 설정, 기본 화면, 실행 검증·문서를 목적별로 구분합니다. 무관한 파일을 섞거나 파일 수만을 기준으로 나누지 않습니다.
- **Android 버전:** 초기 최소 지원은 API 26(Android 8.0)입니다. compile/target API 36, Build Tools 36.0.0으로 고정합니다. 실제 사용자 기기는 아직 확인되지 않았습니다.
- **호환되는 안정 버전 고정:** 설치된 SDK 36과 검증 가능한 AGP 8 계열에 맞춰 AGP 8.13.2, Gradle 8.14.4, Kotlin 2.2.21, Compose BOM 2026.02.01, Activity 1.12.4, Navigation 2.9.8을 선택합니다. 최신 버전을 자동 추적하지 않습니다. Compose compiler plugin과 Kotlin 버전은 일치시킵니다.
- **JDK:** Gradle 실행은 설치된 Corretto 21, Java/Kotlin 출력은 17로 통일합니다. PC별 JDK 절대 경로는 저장소에 넣지 않습니다.
- **Windows 한글 경로:** 사용자 작업 경로를 유지하기 위해 `android.overridePathCheck=true`를 사용하고 실제 빌드 결과로 확인합니다.
- **가로 화면 우선:** 태블릿 가로 화면에서는 두 열, 작은 화면과 글자 확대에서는 한 열로 배치합니다. 회전을 강제 고정하지 않고 시스템 방향과 창 크기에 적응합니다. 모든 내용은 스크롤할 수 있고 버튼은 최소 64dp입니다.
- **로컬 저장 원칙:** 네트워크 권한과 서버 SDK를 추가하지 않습니다. 백업을 비활성화합니다. Room/DataStore 및 민감하지 않은 학습 기록의 명시적인 백업 정책은 저장 기능 PR에서 구체화합니다.
- **외부 자산:** 폰트는 기기 기본 폰트, 앱 아이콘은 직접 작성한 VectorDrawable입니다. 다운로드한 이미지·음원은 없습니다.
- **개발 통합 브랜치:** 사용자 후속 지시에 따라 개발 중인 PR의 대상은 `develop`으로 변경합니다. 기능·문서 브랜치는 최신 `develop`에서 만들고 해당 브랜치로 PR을 보냅니다. 기존 원격 `develop`은 초기 `main`과 같은 커밋에서 시작합니다. PR #1도 `develop`을 대상으로 변경합니다.
- **완성본 반영:** `main`에는 앱 완성 및 최종 검증 후 별도 PR로 반영합니다. 개발 중간 결과는 `develop`에 모읍니다.
- **다음 단계:** 본 PR 검토·병합 후 최신 `develop`에서 문서 PR을 시작해 난이도 규칙 예제, 복원 이력, AI 보상 연결, 데이터 트랜잭션을 구체화합니다. 자동 병합하지 않습니다.

## 2026-09-22 · 설계 문서 PR

- **사용자 요청:** 구현·검증·목적별 커밋·PR은 합의한 범위에서 이어서 진행합니다. 앞 PR이 develop에 병합된 뒤 다음 기능 브랜치를 만들며 자동 병합은 하지 않습니다.
- **디자인 선택:** 사용자가 ‘차분한 기록형: 절제된 색상, 학습 기록과 진행 상황 중심’을 선택했습니다. 현재 홈은 임시안이며 파스텔 카드 반복과 감성 문구 중심 구성을 변경합니다. 실제 화면 수정은 게임 화면·홈 PR에서 적용하고 이 PR은 문서만 변경합니다.
- **종합 훈련 재확인:** 홈에서 직접 시작하는 주요 경로로 제공합니다. 혼합10문제(4/3/3),4개 유형 순환, 유형별 난이도·누적 묶음 공유를 유지합니다. 유형별 훈련도 함께 제공합니다.
- **회전 기준 구체화:** 완료한 종합 사이클 수를 기준으로 색→그림→숫자가4개 배정을 받습니다. 중단·재개와 유형별 훈련은 순환 카운터를 증가시키지 않습니다.
- **복원 해석:** 부분 복원은 최신 이력의 원래 단축량이 아니라 현재 남은 단축량을 기준으로 합니다. 한 번의 하락은 축별 최신 이력 하나까지만 복원합니다. 대기3초 도달이나 풀이40초 도달 후 후속 복원/제한 해제는 다음 판정에서 처리합니다.
- **보상 없는 선택의 점수:** 실제 적용 경험이 있지만 모드 전환으로 보상 없이 종료되어 유효 횟수가0이면, 미시도 행동 탐색 후 +∞ 점수로 평가합니다. 양수 횟수의 UCB 수식은 명세 그대로입니다. underflow 처리도 AI_DESIGN.md에 기록했습니다.
- **알고리즘 모드 변경:** 진행 사이클이 없을 때만 적용합니다. 새 epoch를 만들고 미완성 묶음·미보상 연결은 종료 상태로 보존합니다. 기존 기록·난이도·복원 이력·획득한 밴딧 학습값은 유지합니다. 종합/유형별 훈련 변경에는 이 분리 규칙을 적용하지 않습니다.
- **풀이 마감 경계:** 단조 시각이 마감 미만일 때 답변을 처리하고, 마감 이상이면 시간 초과로 판정합니다. 콜백 도착 순서나 UI tick 횟수에 의존하지 않습니다.
- **저장 책임:** DataStore에는 안내·효과음 설정을 두고 학습 연결에 영향을 주는 실제 알고리즘 모드는 Room epoch로 관리합니다. 문제·묶음·보상·마지막 문제의 사이클 완료를 같은 트랜잭션에서 처리합니다.
- **다음 PR:** 순수 Kotlin 문제 모델·세 유형 생성·고유한 정답·최대3회 판정·주입 시계/난수와 재현 가능한 테스트를 구현합니다. 혼합 사이클·기록 저장·난이도·밴딧은 각각의 예정 PR에서 연결합니다.

## 2026-09-23 · 게임 엔진 PR

- **범위:** 최신 develop의 설계 문서 병합 커밋 `dd00acd`에서 `feature/game-engine`을 만듭니다. 문제 모델·생성·한 문제 진행/판정·JVM 테스트를 구현하며 현재 화면 디자인과 홈 동작은 변경하지 않습니다.
- **색 문제:** 네 기본 색의 좌우 두 칸 조합 16가지를 사용합니다. 명세가 허용한 색 구성 방식으로, 16개 보기를 만들기 위해 비슷한 색을 늘리지 않습니다. 실제 색 구분 사용성은 화면 검증에 남깁니다.
- **그림·숫자:** 그림은 16개 식별자를 정의하고 렌더러는 화면 PR에서 작성합니다. 숫자는 v1에서 10~99 두 자리로 고정합니다. 유형별 생성기 버전과 실제 보기 콘텐츠를 후속 저장소에서 함께 보존합니다.
- **시간·종료:** 표시 콜백부터 각각 기억/풀이 시계를 시작하고 보기 준비는 풀이에 포함하지 않습니다. 늦은 이벤트의 경과 시간은 실제 관측값을 기록합니다. 미완료 중단은 무효이며 완료 결과는 이후 이벤트로 변경하지 않습니다. 세부 호출 계약은 GAME_ENGINE.md를 따릅니다.
- **Windows JVM 테스트:** Gradle 8 테스트 실행 인수 파일이 UTF-8로 쓰이고 Java 실행기가 MS949로 읽어 한글 경로의 테스트 클래스를 찾지 못하는 문제를 재현했습니다. JDK 21의 `file.encoding=COMPAT`로 문자셋을 맞추고 테스트 통과를 확인했습니다. 안내 JDK를 검증한 21로 통일하며 Java 소스 인코딩은 UTF-8로 명시합니다. OS 언어·전역 환경 설정이나 프로젝트 경로는 변경하지 않습니다.
- **다음 PR:** 이 PR이 develop에 병합된 후 게임 화면과 ViewModel·StateFlow, 실제 프레임/단조 시계와 생명주기 연결을 구현합니다. 종합 10문제 사이클·저장·난이도·AI는 후속 PR에서 연결합니다.

## 2026-09-23 · 시작 버튼과 플레이 연결

- **사용자 요청 우선:** 게임을 시작할 수 없다는 후속 요청에 따라 아직 열려 있는 PR #3에 실제 플레이까지 추가합니다. 별도 종속 브랜치나 자동 병합 없이 기존 네 커밋 뒤에 기능별 커밋을 붙입니다.
- **기본 플레이 완결:** 종합/유형별 시작 → 기억 → 대기 → 풀이 → 문제 결과 → 10문제 결과를 연결합니다. 종합의 4/3/3 및 완료 시 순환을 메모리에서 먼저 구현합니다. 유형별 누적 묶음과 적응 난이도는 구현 범위에 포함하지 않습니다.
- **현재 저장 한계:** ViewModel은 Activity 재생성과 홈 왕복 중 진행을 유지하지만 프로세스 종료 후에는 복구되지 않습니다. 홈과 결과 화면에서 종료 시 기록이 사라짐을 알립니다. Room 저장·복구를 다음 단계로 우선합니다.
- **중단·표시:** 화면이 RESUMED일 때만 시계를 진행하고, 대상·보기의 프레임을 거친 후 시계를 시작합니다. ON_PAUSE/화면 이탈은 무효 처리합니다. 이전 화면 정리 콜백이 새 사이클을 중단하지 않게 세션 ID를 검사합니다.
- **시각 구성:** 선택한 차분한 기록형 기준을 홈과 게임에 적용합니다. 16개 그림은 직접 Canvas로 작성하고 이름 설명을 제공하며 외부 자산을 추가하지 않습니다.

## 2026-09-23 · 클래식 메뉴와 기록 보존

- **기준 브랜치:** PR #3이 develop에 병합된 `5388a69`에서 `feature/classic-home-persistent-records`를 만들었습니다. 새 PR의 대상도 develop입니다.
- **최신 디자인 요청 우선:** 메인은 큰 게임 이름과 학습하기·학습 현황·게임 설명 세 버튼입니다. 기록 정보와 게임 시작은 각 화면으로 옮깁니다. 크림색 바탕과 남색, 64sp 제목·30sp 메인 버튼을 적용합니다.
- **정식/연습 구분:** 게임 시작은 혼합 10문제이며 세 유형 버튼은 명시적으로 연습입니다. 기존 명세의 연습 제외 원칙에 따라 Room 기록과 AI 입력에 포함하지 않습니다. 정식 진행 중에 연습해도 정식 기록은 보존합니다.
- **저장 우선:** 사용자 요청에 따라 원본 데이터·완료 진행·4/3/3 순환을 Room으로 영속화합니다. 각 입력의 시각을 먼저 잡고 직렬 이벤트 처리와 트랜잭션을 사용하며 커밋 후 결과를 공개합니다. 실패 시 재시도로 마지막 커밋에서 복구합니다.
- **종료 처리:** 앱 종료 후 미완료 문항은 무효 이력으로 보존하고 같은 조건의 새 문제를 냅니다. 완료 문항은 유지합니다. ‘진행 중인 훈련 끝내기’도 완료 기록을 삭제하지 않고 STOPPED로 남깁니다.
- **스키마:** Room 2.8.4, Kotlin 2.2.21에 맞는 KSP 2.2.21-2.0.5를 고정합니다. [Room 릴리스](https://developer.android.com/jetpack/androidx/releases/room), [KSP 릴리스](https://github.com/google/ksp/releases)를 확인했으며 현재 도구 조합의 실제 빌드를 검증했습니다. 생성 스키마를 커밋하고 파괴적 마이그레이션은 사용하지 않습니다.
- **AI 경계:** 원본 기록은 PENDING_ALGORITHM으로 보존합니다. 유형별 묶음·난이도·밴딧은 후속 구현이며 현재 데이터를 이미 모델 학습에 반영했다고 표시하지 않습니다. 향후 스키마 변경은 기존 원본을 보존해야 합니다.
- **기존 기록:** 이전 버전은 DB가 없어 이미 종료되어 사라진 메모리 기록은 가져올 수 없습니다. 이번 버전부터 기기 내부에 누적합니다. 외부 서버·네트워크 권한·자동 백업을 추가하지 않습니다.

## 2026-09-23 · 게임 화면 중앙 배치와 일시정지

- **후속 요청:** 아직 열려 있는 PR #4에 게임 화면 배치를 추가합니다. 저장 기능과 기존 완료 기록을 유지하고 현재 develop 대상 PR을 갱신합니다.
- **배치:** 단계 안내·큰 그림·시간 숫자를 가운데 정렬하고 다음 버튼은 본문 스크롤 밖의 하단에 고정합니다. 정답은 중앙 문구만, 오답은 남은 기회와 비활성 보기만 표시합니다.
- **이동 메뉴:** 게임 본문의 학습 선택 버튼을 없애고 문제 번호 옆의 일시정지로 이동 동작을 모읍니다. 시스템 뒤로가기 역시 이 메뉴를 엽니다. 뒤로가기는 학습 선택, 홈으로는 메인 메뉴입니다.
- **일시정지 기록:** 메뉴 뒤로 기억 대상/보기가 노출되지 않게 즉시 감춥니다. 기존 중단 정책대로 현재 미완료 문제를 무효 이력으로 남기고 새 문항으로 이어갑니다. 이미 완료한 정답·시도·문제 수는 바꾸지 않습니다. 완료 결과에서 메뉴를 열었다가 계속하면 같은 결과로 돌아옵니다.

## 버전 근거

2026-09-22 공식 문서와 배포물을 확인했습니다.

- [AGP 8.13 호환성](https://developer.android.com/build/releases/agp-8-13-0-release-notes): Gradle 최소 8.13, JDK 최소 17, API 36 지원.
- [Kotlin Gradle plugin 호환성](https://kotlinlang.org/docs/gradle-configure-project.html), [Kotlin 릴리스](https://kotlinlang.org/docs/releases.html).
- [Compose BOM](https://developer.android.com/develop/ui/compose/bom), [버전 매핑](https://developer.android.com/develop/ui/compose/bom/bom-mapping).
- [Activity 릴리스](https://developer.android.com/jetpack/androidx/releases/activity), [Navigation 릴리스](https://developer.android.com/jetpack/androidx/releases/navigation).
- [Gradle 배포 체크섬](https://services.gradle.org/distributions/gradle-8.14.4-bin.zip.sha256).
